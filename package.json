{
  "name": "quiz-api",
  "version": "1.0.0",
  "private": true,
  "description": "test project for quiz registration API",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^26.2.0",
    "dotenv": "^17.4.2",
    "prettier": "^3.9.6",
    "typescript": "^5.5.0"
  }
}
